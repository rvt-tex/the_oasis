
<%= form_for([@review.appointment,@review]) do |f|%>

<% if !@review.appointment %>
  <%= f.label :appointment %>
  <%= f.collection_select :appointment_id, current_client.appointments.all, :id, :desired_date %>
<% else %>
  <%= f.hidden_field :appointment_id %>
<% end %>

<br><br>

<%= f.label :comment %><br>
<%= f.text_area :comment %>

<br><br>

  <%= f.submit %>

<% end %>	